{
  "name": "cinematica",
  "version": "1.0.0",
  "description": "Cinematica — MERN stack movie review landing page",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "start": "concurrently \"npm start -w backend\" \"npm start -w frontend\"",
    "build": "npm run build -w frontend",
    "install-all": "npm install && npm install -w backend && npm install -w frontend"
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": ["mern", "react", "express", "mongodb", "movies"],
  "license": "MIT"
}

